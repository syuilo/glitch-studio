# AGENTS.md for Glitch Studio

## Project Context

- Glitch Studioは、ノードベースのエディタを通じてリアルタイムな画像・動画・音声の加工・編集を行ったり、シェーダーのplaygroundとして使えるWebアプリケーションです。
- WebWorkers上で動くWebGPUを用いたレンダラーを、Command patternを使用して状態管理を行うVue製WebUIから操作する構成です。
- レンダラーにはノード情報が与えられます。各ノードは、それがどのような処理(エフェクト)を行うかという情報や、エフェクトに対するパラメータ情報(他のノードへの接続情報も含む)などを持ちます。
- ユーザーは、各パラメータをUI上のスライダーなどを通じて設定できるほか、パラメータに式を記述することもできます。そのような式を評価したりする作業もレンダラーの役目です。
- パラメータの型は、数値、真理値、色、ベクトル、ノード入力など様々です。
- パーリンノイズなど、画像以外のデータ(スカラー場やベクトル場)を生成するエフェクトもあります。そのようなデータは直接最終結果に表示するというよりは、別のエフェクトの入力として使用することが想定されています。
- 複数の出力をもつエフェクトも存在し得ます。
- 画像や動画を読み込んだり、Webカメラからの映像をノードに入力することもできます。
- 音声のビジュアライゼーションを行うエフェクトもあります。
- マクロ(ノード全体で参照できる変数のようなもの)を定義する機能もあります。
- ノードをグループ化(カプセル化)する機能もあります。
- UIにもこだわっていて、複数のパネルを自由なレイアウトで配置したり、プレビューを別ウィンドウで開けたり、ヒストグラム・波形を表示する機能があったりします。
- レンダリングのパフォーマンス(フレームのレンダリングにかかった時間等)を計測する機能もあります。
- ノードの入力が前回のレンダリング時から変化がない場合は、そのノードの処理をスキップしてパフォーマンスを向上させる仕組みがあります。
- まだ開発初期段階のため、未実装の機能や動かない機能があります。
- 最新のChromeを動作対象としています。サポートが限られる最新の構文やAPIを使うことを厭いません。
- デスクトップPCで動かすことを想定しています。ただしスマホやタブレット対応を諦めているわけではなく、ポインターをマウスと決めつけずタッチであることも考慮するなど、それらの環境への配慮も可能であれば行います。ただし優先順位は低いです。
- 今後も様々な機能を実装予定です。
- Tech Stack: TypeScript, Vite, Vue 3, WebGPU, WGSL, WebAudio, WebWorker, CSS Modules
- Keywords: moduler, routing, signals, wiring, nodes, graph, chain, pipeline, compositor, playground, visualization, interactive performance

## Directory Structure

モノリポになっています。

- [**`shared`**](./packages/shared) ... `renderer`からも`ui`からも使用される共通の処理や定義などが含まれます。`renderer`、`ui`を参照することはありません。
  - effect-definitions ... 各種エフェクトのパラメータ定義などが含まれます。パラメータ定義はレンダラーからもUIからも参照される情報なのでここに置かれています。1つのエフェクトに1つのファイルを割り当てます。
- [**`renderer`**](./packages/renderer) ... レンダラー実装や各種エフェクト実装が含まれます。レンダラーはノード定義を受け取り、ノード・パラメータの評価を行いWebGPUを用いてレンダリングを行います。なお、レンダラーはWebWorkers上で動作し、オフスクリーンでレンダリングを行うことができます。そのため、DOMに依存しない実装になっています。`ui`を参照することはありません。
  - effect-implementations ... 各種エフェクトの処理本体実装が含まれます。シェーダーコードや、uniformsやバッファなどの準備処理も含みます。レンダラー内部でのみ使用されます。1つのエフェクトに1つのディレクトリを割り当てます。
- [**`ui`**](./packages/ui) ... UI実装が含まれます。状態の管理・レンダラーの呼び出し、表示、ファイルの入出力などを行います。状態の操作は原則Command patternで行うようになっていてUndo/Redoを可能にしています。
  - engine.ts ... rendererのラッパーのような役割で、レンダラーがワーカー越しに動いていることを隠蔽しつつレンダラーの管理を行います。
  - app.ts ... アプリケーションのステートの管理などを行います。
  - commands.ts ... ステートに対する操作(コマンド)の実装をまとめています。

なお、rendererはあくまでも(画像の)ノード・エフェクトとその解析(ヒストグラム等)のレンダリングを担当するもので、UI上の音声のビジュアライゼーション(スペクトログラム等)のレンダリングを担当するものではありません。それらはUI側の責務です。(ui package内に収めろという意味ではなく、UI側が責任をもって管理するのであれば別パッケージを作りそれをUIが依存する設計にするのは何の問題もありません)

もちろん、入力された音声信号を視覚化するエフェクトはあり、それは正しい実装です。画像ノード・エフェクトとは関係しない音声のみのレンダリングは担当外という意味です。

つまり、「renderer」は「(全般的な)renderer」ではなく、「(node) renderer」であると捉えてください。"node" rendererに対して、画像ノードとは関係しないプレビュー用音声のスペクトログラムのレンダリングの実装を入れる・レンダリングを依頼するのはおかしいということが解ると思います。

そうしないと、音声のプレビュー用レンダリングがレンダラーの解像度設定やFPS設定に影響を受けるといったおかしな実装になってしまいます。

## Concepts

### メディア入力の扱い

Glitch Studioのメディアの扱いにあたっては、以下の概念があります。

- **Asset** ... プロジェクトに読み込んだ画像ファイル、動画ファイル、音声ファイルなどの素材
- **Player** ... 動画・音声Assetや、Webカメラ、ライブストリーム(URL)などのメディアソースを参照し、UI上で再生コントロールを提供する
- **Node** ... Player(のID)を参照し、映像や音声を使って描画する。画像は、静的であってコントロールが不要なので直接Asset(のID)を参照する

同じAssetでもそれに対応するPlayerは複数存在し得ます(同じAssetをそれぞれ異なった再生位置で複数再生させるなど)。

同じPlayerでもそれに対応するNodeは複数存在し得ます(同じPlayerでもフレーム描画エフェクトと音声波形描画エフェクトなど、複数のNodeから参照するなど)。

## Design

シェーダーの実装・レンダラーの設計方針については、原則以下のようになっています。

- **resolution-independent** ... どのような解像度でレンダリングしても見た目が変わらない。
- **fps-independent** ... どのようなフレームレートでレンダリングしても動き・変化の速さが変わらない。
- **aspect-ratio-independent** ... どのような比率でレンダリングしても歪まない。

ただし、一部例外もあるため、絶対ではありません。

また、レンダラーおよび各エフェクトは、基本的にdeterministicであることが求められます。そうでないと、(将来実装予定の)時間範囲を指定して動画としてレンダリングを行ったときなどに、レンダリングするたびに結果が変わってしまうことになり得ます。

座標系は、左下が[-1, -1]、右上が[+1, +1]です。中央が[0, 0]です。

現在は、原則ノード間で受け渡しされるテクスチャのサイズは共通となっていますが、将来的にノードごとに異なる解像度や比率でテクスチャを出力できるようになる可能性はあるので、「テクスチャの比率は全て同じである」という前提のコードは書くべきではありません。
常に自身のテクスチャのアスペクト比と入力テクスチャのアスペクト比をシェーダーに渡し、指定されたfit方法で補正する実装になっていることが理想です。

ノード間の接続は、deterministicです。つまり、expressionで動的に配線を変えるといった挙動はサポートしません。

UIに関しては、メンテナンスしやすさを保つため、HTMLをセマンティックにしたり、アクセシビリティを考慮したりする必要はありません。基本的にdivやspanタグを使い、aria属性・role属性は不要です。

### アルファの扱い

画像ノード間で受け渡すテクスチャは、premultiplied alpha（RGBにアルファを乗算済みの形式）に統一します。各画像ノードは乗算済みの入力を受け取り、乗算済みの出力を返します。各ノードで毎回 `rgb *= alpha` を行うという意味ではなく、二重乗算を避ける必要があります。

- 画像・動画の入力や色の生成では、未乗算の色をpremultiplyして出力します。読み込み時に既に乗算されている場合は、再度乗算しません。補間やミップマップ生成も乗算済みの形式で行う必要があるため、入力の変換はそれらの処理より前に行います。
- 移動・変形では乗算済みのRGBAをそのまま扱い、ブラー・補間ではそのRGBAをまとめて平均します。未乗算のRGBを平均すると、透明画素の色がにじむ原因になります。
- 色相変更など、未乗算のRGBが必要な処理では、必要に応じてunpremultiply → 加工 → premultiplyを行います。アルファが0の場合は分岐し、0除算を避けます。
- 最終レンダリングでは追加のpremultiplyを行わず、そのまま出力します。Canvasの `alphaMode: 'premultiplied'` は出力値を乗算済みとして解釈する指定であり、自動で乗算する機能ではありません。
- スカラー場・ベクトル場などのデータ用テクスチャは、この色の規約の対象外です。各チャンネルをデータとして扱い、アルファによる乗算を行いません。

この方針を採る理由は以下のとおりです。

- このアプリではブラー・変形・縮小などによって途中のノードでも画素を混ぜるため、最終レンダリングで一度だけpremultiplyする方法では不十分です。未乗算のまま補間すると、完全に透明な画素のRGBまで結果に混ざり、輪郭に色や黒がにじみます。一度混ざった色は、最終段でアルファを乗算しても修復できません。
- 例えば、不透明な赤 `(1, 0, 0, 1)` と透明な黒 `(0, 0, 0, 0)` の中間は、乗算済みなら `(0.5, 0, 0, 0.5)` で、半透明の赤を正しく表します。これを未乗算の色として扱って最後にpremultiplyすると `(0.25, 0, 0, 0.5)` となり、暗い輪郭になってしまいます。
- ノード間の形式を統一すれば、各エフェクトは入力元の種類を意識せずに処理・合成できます。ノードの無効化によるバイパスでも、入力テクスチャを変換せずそのまま後段へ渡せます。
- 乗算済みのRGBに再びアルファを掛けると、透明度が同じまま色だけが暗くなります。二重乗算を禁止することで、何も色を変えないノードを追加しただけで見た目が変わることを防ぎます。
- データ用テクスチャのチャンネルは色や不透明度を表すとは限りません。そこにpremultiplyを適用すると、スカラー値やベクトルの大きさなど、後段が利用するデータ自体が変わってしまいます。

### 浮動小数点テクスチャの精度とサンプリング

浮動小数点のデータ出力・内部計算・履歴用テクスチャは、全エフェクトで `wgpu.enable32bitDataTextures` に応じて保存形式を統一します。

| 用途 | false | true |
| --- | --- | --- |
| スカラー場 | `r16float` | `r32float` |
| 2成分ベクトル場 | `rg16float` | `rg32float` |
| 4成分データ・浮動小数点の履歴 | `rgba16float` | `rgba32float` |

- このフラグはアダプターの対応可否だけを表すものではありません。trueの場合はデバイス作成時に `float32-filterable` を `requiredFeatures` に含め、有効化済みであることが前提です。falseのときに32bit floatのノード出力を生成してはいけません。
- 通常の画像出力は引き続き `wgpu.intermediateTextureFormat` に従います。この設定は画像の保存・ブレンド用の設定であり、データ用テクスチャの精度設定とは別です。`float32-filterable` は32bit floatのハードウェアブレンドを保証しないため、画像の中間形式を無条件に `rgba32float` に変更しないでください。
- 出力だけでなく、内部の一時テクスチャ・履歴・compute shaderのstorage textureにも精度設定を適用します。テクスチャの生成形式、render pipelineのtarget、bind group layoutのstorage形式、WGSLのstorage texture宣言は必ず一致させます。出力チャンネル数も合わせてください。
- `canNode: true` のパラメータは、定数なら1x1テクスチャ、接続なら接続元の出力テクスチャを受け取ります。接続時に暗黙の形式変換があると仮定しないでください。この形式統一により、通常の浮動小数点入力をフィルタリング可能な入力として扱えます。
- CPUから16bit floatへアップロードするときは、数値をhalfのビット表現に変換します。`Float32Array` のバイト列をそのまま渡してはいけません。`bytesPerRow` も保存形式に合わせます。共通の変換処理は `packages/shared/src/utility/float32ToFloat16Bits.ts` にあります。
- 16bit化では丸めと表現範囲の縮小が発生します。履歴との差分を計算する処理では、現在値と履歴値を同じ保存精度に揃えて、静止入力に丸め由来の差分が出ないようにします。蓄積では保存可能な範囲への制限などによりオーバーフローを防ぎます。

**読み取りは原則サンプラーを使います。** 画像・連続したスカラー場・ベクトル場の拡大縮小や `canNode` パラメータの読み取りには、線形フィルタリングを設定したサンプラーと `textureSample` を使います。bind group layoutではテクスチャを `sampleType: 'float'`、サンプラーを `type: 'filtering'` とします。32bit float非対応を回避する目的の手動双線形補間は不要です。

- `textureSample` は暗黙の微分を使うため、fragment shaderのuniformな制御フローで呼びます。画素ごとに異なる分岐や早期returnの後、compute shader、LODを固定する必要がある処理では `textureSampleLevel(..., 0.0)` を使います。これは同じサンプラーによる読み取りであり、最近傍読み取りを意味しません。
- UVは各入力自身のサイズ・比率に基づいて計算し、指定されたfit方法を適用します。パラメータをstretchで対応付ける場合も、その方針を明確にしてください。1x1の定数入力や、出力と異なる解像度の入力も扱います。
- サンプラーの境界処理は用途に合わせます。通常のパラメータ入力は `clamp-to-edge`、画像のrepeat等は指定されたwrap方法を使います。画像用のrepeatサンプラーをパラメータにも流用して境界挙動を変えないでください。containや透明領域の処理は別途維持します。
- `textureLoad` は禁止ではありません。履歴の同一画素の蓄積・比較、画素ソート、整数グリッド上の差分計算、ヒストグラム・波形集計など、補間すると意味が変わる処理には残します。なぜ整数座標で厳密に読む必要があるかをコメントに残してください。単に32bit floatを読むため、という理由では使用しません。
- 音声プレビューはnode rendererから独立した責務です。共通の音声描画処理が精度設定を受け取る場合、ノード側は自分の設定を渡し、UI側は独立した設定を使います。UIの音声描画をnode rendererの設定・FPS・解像度に従属させないでください。

## Advice

- 一般的にインターネット上では、WGSLで書かれたシェーダーはまだ少ないと思われるため、シェーダーを書くときは一旦GLSLで考えて(情報を探して)から、それをWGSLに変換するようなイメージだと書きやすいかもしれません。ただしWGSL特有のパフォーマンス向上方法や、新しい関数などがあれば使用してください。もちろん、既にWGSLの十分な知識があるのであれば、最初からWGSLを基準に考えてよいです。
- 一見すると分かりにくかったり、意図を残すことが重要と思われるような処理を書くときは、なるべく日本語でコメントを残すようにしてください。
  - 特に複雑な数式などは理解しにくくなりがちなので積極的にコメントを残しましょう。
- 変数名も、多少長くなったとしても、曖昧性がなく明確で説明的、それでいて簡潔な名前を心掛けましょう。
- 安易に処理を共通化しないでください。かえって実装が複雑になり理解が難しくなります。
- まだ開発初期段階であり、正式リリースされておらずユーザーが存在するプロダクトではないため、データ構造などを変更したときに過去のデータとの互換性を持たせるための追加実装などは不要です。
